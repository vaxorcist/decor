<%# decor/app/views/owners/appliances.html.erb - version 1.2 %>
<%# v1.2 (Session 38): Added Connections tab (inactive) to the tab strip. %>
<%# v1.1 (Session 25): Added Peripherals tab to the tab strip (inactive). %>
<%#   Tab order: Computers | Appliances (active) | Peripherals | Components. %>
<%# v1.0 (Session 23): New sub-page for the owner's appliances. %>
<%#   Renders the shared _profile partial, a tab strip to navigate between %>
<%#   the sub-pages, and the appliances table (device_type: appliance only). %>
<%#   Table content and column layout extracted from owners/show.html.erb v1.8. %>

<div class="max-w-7xl mx-auto">
  <%# Profile header + info panel %>
  <%= render "profile" %>

  <%# Tab strip — switches between the four owner sub-pages.
      Active tab: border-indigo-600 + text-indigo-700 + font-medium.
      Inactive tabs: border-transparent + text-stone-600. %>
  <div class="flex gap-6 border-b border-stone-200 mb-6">
    <%= link_to "Computers",  computers_owner_path(@owner),
          class: "pb-2 -mb-px border-b-2 " \
                 "border-transparent text-stone-600 hover:text-stone-900" %>
    <%= link_to "Appliances", appliances_owner_path(@owner),
          class: "pb-2 -mb-px border-b-2 font-medium " \
                 "border-indigo-600 text-indigo-700" %>
    <%= link_to "Peripherals", peripherals_owner_path(@owner),
          class: "pb-2 -mb-px border-b-2 " \
                 "border-transparent text-stone-600 hover:text-stone-900" %>
    <%= link_to "Components", components_owner_path(@owner),
          class: "pb-2 -mb-px border-b-2 " \
                 "border-transparent text-stone-600 hover:text-stone-900" %>
    <%= link_to "Connections", connections_owner_path(@owner),
          class: "pb-2 -mb-px border-b-2 border-transparent text-stone-600 hover:text-stone-900" %>
  </div>

  <%# Section header %>
  <div class="flex justify-between items-center mb-3">
    <h2 class="text-lg font-semibold">Appliances (<%= @appliances.count %>)</h2>
    <% if Current.owner == @owner %>
      <%= link_to "Add Appliance", new_computer_path(device_type: "appliance"),
            class: button_classes(style: :secondary) %>
    <% end %>
  </div>

  <% if @appliances.any? %>
    <div class="bg-white border border-stone-200 rounded overflow-auto"
         style="max-height: calc(100vh - 18rem);">
      <table class="min-w-full divide-y divide-stone-200">
        <thead class="bg-stone-50 sticky top-0 z-10">
          <tr>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Model</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Order</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Serial</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Condition</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Status</th>
            <%# Trade header — visible to logged-in members only %>
            <% if logged_in? %>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Trade</th>
            <% end %>
            <th class="px-4 py-2"></th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-stone-200 text-sm">
          <% @appliances.each do |appliance| %>
            <tr class="hover:bg-stone-50">
              <%# Model — clickable; appliances use the computers_* routes %>
              <td class="px-4 py-3 whitespace-nowrap font-medium">
                <%= link_to appliance.computer_model.name, computer_path(appliance),
                      class: "text-indigo-600 hover:text-indigo-900" %>
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= appliance.order_number || "—" %></td>
              <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= appliance.serial_number %></td>
              <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= appliance.computer_condition&.name || "—" %></td>
              <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= appliance.run_status&.name || "—" %></td>
              <%# Trade cell — visible to logged-in members only %>
              <% if logged_in? %>
                <td class="px-4 py-3 whitespace-nowrap">
                  <% if appliance.barter_status_offered? %>
                    <span class="text-green-700">Offered</span>
                  <% elsif appliance.barter_status_wanted? %>
                    <span class="text-amber-600">Wanted</span>
                  <% else %>
                    <span class="text-stone-400">—</span>
                  <% end %>
                </td>
              <% end %>
              <%# Actions — Edit and Delete shown to owner only %>
              <td class="px-4 py-3 whitespace-nowrap text-right">
                <% if Current.owner == @owner %>
                  <div class="flex items-center justify-end gap-3">
                    <%= link_to "Edit", edit_computer_path(appliance),
                          class: "text-indigo-600 hover:text-indigo-900" %>
                    <%= button_to "Delete", computer_path(appliance), method: :delete,
                          data: { turbo_confirm: "Delete #{appliance.computer_model.name} (#{appliance.serial_number})? This cannot be undone."},
                          params: { source: "owner" },
                          class: "text-red-600 hover:text-red-900 bg-transparent border-0 p-0 cursor-pointer",
                          form: { style: "display:inline" } %>
                  </div>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="text-sm text-stone-500">No appliances registered.</p>
  <% end %>
</div>
