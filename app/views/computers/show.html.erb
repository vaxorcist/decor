<%# decor/app/views/computers/show.html.erb - version 2.5 %>
<%# v2.5 (Session C, Storage Locations feature, Part 3 of 6): Added a %>
<%#   Storage Location field. This page is PUBLIC (visitors and other %>
<%#   owners can view any computer's show page), but Storage Location is %>
<%#   private data (storage_location.rb v1.1's own header comment) — so, %>
<%#   unlike Trade Status (which uses "if logged_in?"), this field is %>
<%#   wrapped in the STRICTER "if Current.owner == @computer.owner || %>
<%#   admin?" guard already used for the Edit/Delete links at the top of %>
<%#   this page. Placed as its own standalone single-field row (like %>
<%#   History) rather than folded into the existing Line 2 grid, since %>
<%#   Line 2's column count already depends on logged_in? (3 vs 2 cols for %>
<%#   Trade Status) and adding a second, DIFFERENT conditional (owner-only) %>
<%#   into that same grid would make the column count logic depend on two %>
<%#   independent booleans at once. This is a first pass consistent with %>
<%#   the Session 79 design consultation's privacy answer, not a %>
<%#   substitute for DECOR_PROJECT.md Session D's dedicated privacy audit. %>
<%# v2.4 (Session 77): Same "single source of truth / touch N places, miss %>
<%#   one" shape as v2.3 below and RAILS_SPECIFICS.md's Session 73 rule — %>
<%#   the Components sub-table on THIS page (further down, a separate %>
<%#   table from the Computer-level fields v2.3 already fixed) was still %>
<%#   missing an Owner Part No. column entirely, even though the %>
<%#   equivalent sub-table in computers/_form.html.erb gained one back in %>
<%#   Session 71 (v2.8). Added "Owner Part No." between "Order No." and %>
<%#   "Serial No.", matching the column order established in %>
<%#   computers/_form.html.erb v2.8's own header comment ("Column placed %>
<%#   between 'Order No.' and 'Serial No.', matching the field order %>
<%#   established in components/_form.html.erb v1.12 and %>
<%#   owners/components.html.erb v1.6"). No controller change needed — %>
<%#   @components is already eager-loaded and owner_part_number is already %>
<%#   a plain column read, same pattern as order_number/serial_number two %>
<%#   lines below. %>
<%# v2.3 (Session 75, pending): Two bugs fixed, both from the same root %>
<%#   cause — this show page was never updated alongside the Session 70/71/74 %>
<%#   Owner Part Number and device_type-label fixes that _form.html.erb (v2.7, %>
<%#   v2.8, v2.9) already received. Same "single source of truth / touch N %>
<%#   places, miss one" shape as RAILS_SPECIFICS.md's Session 73 rule — the %>
<%#   fixes landed on the edit/new form but not the show page. %>
<%#   1. Line 1 was still grid-cols-3 (Model | DEC Part Number | DEC Serial %>
<%#      Number) with no Owner Part Number field at all. Widened to %>
<%#      grid-cols-4 and added a 4th column, matching _form.html.erb v2.9's %>
<%#      field ORDER exactly (Owner Part Number placed after DEC Serial %>
<%#      Number, not between DEC Part/Serial Number — confirmed by reading %>
<%#      the actual form file rather than assuming the components/_form.html.erb %>
<%#      ordering carried over). %>
<%#   2. The Model dt label was hardcoded "Computer Model" even when %>
<%#      @computer.device_type is "peripheral". Fixed to use the device's %>
<%#      capitalized device_type value in the label, matching the pattern %>
<%#      already used in _form.html.erb v2.9 and new.html.erb v1.5. %>
<%# v2.2 (Session 47): Software feature Session E. %>
<%#   Added read-only Software section after the Connections section. %>
<%#   Columns: Name (link to software_items/show) | Version | Condition. %>
<%#   Add/Edit/Delete actions on this page are deferred (Session F). %>
<%#   @software_items loaded by the controller (eager-loaded :software_name, %>
<%#   :software_condition, ordered by created_at ascending). %>
<%# v2.1 (Session 38): Connections section redesigned — multi-row table. %>
<%#   Columns: CONNECTION ID | TYPE | CONNECTION LABEL | PORT ID | CONNECTS | PORT LABEL. %>
<%#   First row of each group shows all group columns; subsequent rows show only %>
<%#   the port columns (PORT ID, CONNECTS, PORT LABEL). %>
<%#   Groups ordered by owner_group_id; ports by owner_member_id. %>
<%#   Controller (v1.18) updated eager-load to include connection_members with computer+model. %>
<%# v2.0 (Session 36): Connections Type column now always shows connection_type.name. %>
<%# v1.9 (Session 35): Brakeman fix — peer links built with safe_join. %>
<%# v1.8 (Session 34): Added read-only Connections section. %>
<%# v1.7 (Session 22): Added Trade Status field. %>

<div class="max-w-7xl mx-auto">

  <%# ── Header: title + owner (left), Edit/Delete (right, owner/admin only) ── %>
  <div class="flex justify-between items-start mb-4">
    <div>
      <h1 class="text-2xl font-semibold"><%= @computer.computer_model.name %></h1>
      <p class="text-sm text-stone-600">Owned by <%= link_to @computer.owner.user_name, @computer.owner, class: "font-medium underline text-stone-700 hover:text-stone-900" %></p>
    </div>
    <% if Current.owner == @computer.owner || admin? %>
      <div class="flex gap-2">
        <%= link_to "Edit", edit_computer_path(@computer), class: button_classes(style: :primary) %>
        <%= button_to "Delete", computer_path(@computer), method: :delete, data: { turbo_confirm: "Are you sure?" }, class: button_classes(style: :secondary) %>
      </div>
    <% end %>
  </div>

  <%# ── Computer fields ── %>
  <dl class="space-y-4 text-sm mb-6">

    <%# Line 1 (4-col): Model | DEC Part Number | DEC Serial Number | Owner Part Number %>
    <%# Widened Session 75 from grid-cols-3 to grid-cols-4 to fit Owner Part Number — %>
    <%# matches _form.html.erb v2.9's Line 1 exactly (field order and grid width). %>
    <div class="grid grid-cols-4 gap-4">
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1"><%= @computer.device_type.capitalize %> Model</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.computer_model.name %></dd>
      </div>
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">DEC Part Number</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.order_number.presence || "—" %></dd>
      </div>
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">DEC Serial Number</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.serial_number %></dd>
      </div>
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">Owner Part Number</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.owner_part_number %></dd>
      </div>
    </div>

    <%# Line 2: Condition | Run Status | Trade Status (3-col when logged in, 2-col otherwise) %>
    <div class="grid <%= logged_in? ? "grid-cols-3" : "grid-cols-2" %> gap-4">
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">Condition</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.computer_condition&.name || "—" %></dd>
      </div>
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">Run Status</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.run_status&.name || "—" %></dd>
      </div>
      <% if logged_in? %>
        <div>
          <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">Trade Status</dt>
          <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm">
            <% if @computer.barter_status_offered? %>
              <span class="text-green-700">Offered</span>
            <% elsif @computer.barter_status_wanted? %>
              <span class="text-amber-600">Wanted</span>
            <% else %>
              <span class="text-stone-400">—</span>
            <% end %>
          </dd>
        </div>
      <% end %>
    </div>

    <%# Storage Location — Session C. Private field: only the owning owner %>
    <%# or an admin may see it, since this page itself is public. Standalone %>
    <%# row (not part of the Line 2 grid — see v2.5 header comment above). %>
    <% if Current.owner == @computer.owner || admin? %>
      <div>
        <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">Storage Location</dt>
        <dd class="flex items-center w-full h-10 p-3 rounded border border-stone-300 bg-white text-sm"><%= @computer.storage_location&.name || "—" %></dd>
      </div>
    <% end %>

    <%# Line 3 (full): History %>
    <div>
      <dt class="text-xs font-medium text-stone-500 uppercase tracking-wider mb-1">History</dt>
      <dd class="block w-full p-3 rounded border border-stone-300 bg-white text-sm whitespace-pre-wrap" style="min-height: 4.5rem;"><%= @computer.history.presence || "—" %></dd>
    </div>

  </dl>

  <%# ── Components section ── %>
  <div>
    <div class="flex justify-between items-center mb-3">
      <h2 class="text-lg font-semibold">Components (<%= @components.count %>)</h2>
      <% if Current.owner == @computer.owner || admin? %>
        <%= link_to "Add Component", new_component_path(computer_id: @computer.id), class: button_classes(style: :secondary) %>
      <% end %>
    </div>

    <% if @components.any? %>
      <div class="bg-white border border-stone-200 rounded">
        <table class="min-w-full divide-y divide-stone-200">
          <thead class="bg-stone-50">
            <tr>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Type</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Order No.</th>
              <%# Owner Part No. — Session 77. Placed between Order No. and Serial No., %>
              <%# matching computers/_form.html.erb v2.8's established column order. %>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Owner Part No.</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Serial No.</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Description</th>
              <th class="px-4 py-2"></th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-stone-200 text-sm">
            <% @components.each do |component| %>
              <tr class="hover:bg-stone-50">
                <td class="px-4 py-3 whitespace-nowrap font-medium">
                  <%= link_to component.component_type.name, component_path(component), class: "text-indigo-600 hover:text-indigo-900" %>
                </td>
                <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= component.order_number.presence || "—" %></td>
                <%# Owner Part No. — Session 77. NOT NULL, defaults to "-" server-side. %>
                <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= component.owner_part_number %></td>
                <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= component.serial_number.presence || "—" %></td>
                <td class="px-4 py-3 text-stone-600"><%= truncate(component.description, length: 80) || "—" %></td>
                <td class="px-4 py-3 whitespace-nowrap text-right">
                  <% if Current.owner == @computer.owner || admin? %>
                    <div class="flex items-center justify-end gap-3">
                      <%= link_to "Edit", edit_component_path(component), class: "text-indigo-600 hover:text-indigo-900" %>
                      <%= button_to "Delete", component_path(component), method: :delete,
                            data: { turbo_confirm: "Delete this #{component.component_type.name}? This cannot be undone." },
                            params: { source: "computer_show" },
                            class: "text-red-600 hover:text-red-900 bg-transparent border-0 p-0 cursor-pointer",
                            form: { style: "display:inline" } %>
                    </div>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-sm text-stone-500">No components attached to this <%= @computer.device_type %>.</p>
    <% end %>
  </div>

  <%# ── Connections section (read-only) ── %>
  <%# Shows all connections this device belongs to. %>
  <%# Columns: CONNECTION ID | TYPE | CONNECTION LABEL | PORT ID | CONNECTS | PORT LABEL %>
  <%# One row per port; first row of each group shows the group-level columns. %>
  <%# @connection_groups eager-loads connection_members with computer+model (controller v1.18). %>
  <%# Members sorted in memory by owner_member_id — no extra DB queries per group. %>
  <div class="mt-6">
    <h2 class="text-lg font-semibold mb-3">Connections (<%= @connection_groups.count %>)</h2>

    <% if @connection_groups.any? %>
      <div class="bg-white border border-stone-200 rounded">
        <table class="min-w-full divide-y divide-stone-200">
          <thead class="bg-stone-50">
            <tr>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Connection ID</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Type</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Connection Label</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Port ID</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Connects</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Port Label</th>
            </tr>
          </thead>
          <tbody class="bg-white text-sm">

            <% @connection_groups.each_with_index do |group, group_idx| %>
              <% members = group.connection_members.sort_by(&:owner_member_id) %>

              <% members.each_with_index do |member, member_idx| %>
                <tr class="hover:bg-stone-50 <%= 'border-t-2 border-stone-300' if group_idx > 0 && member_idx == 0 %> <%= 'border-t border-stone-100' if member_idx > 0 %>">

                  <%# Group columns — first row of each group only. %>
                  <% if member_idx == 0 %>
                    <td class="px-4 py-3 whitespace-nowrap text-stone-600 align-top font-medium">
                      <%= group.owner_group_id %>
                    </td>
                    <td class="px-4 py-3 whitespace-nowrap text-stone-600 align-top">
                      <%= group.connection_type&.name || "—" %>
                    </td>
                    <td class="px-4 py-3 whitespace-nowrap text-stone-600 align-top">
                      <%= group.label.presence || "—" %>
                    </td>
                  <% else %>
                    <td></td><td></td><td></td>
                  <% end %>

                  <%# Port columns — every row. %>
                  <td class="px-4 py-3 whitespace-nowrap text-stone-600">
                    <%= member.owner_member_id %>
                  </td>
                  <td class="px-4 py-3 text-stone-600">
                    <%= link_to "#{member.computer.computer_model.name} – #{member.computer.serial_number}",
                                computer_path(member.computer),
                                class: "text-indigo-600 hover:text-indigo-900" %>
                  </td>
                  <td class="px-4 py-3 text-stone-600">
                    <%= member.label.presence || "" %>
                  </td>

                </tr>
              <% end %>
            <% end %>

          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-sm text-stone-500">No connections recorded for this <%= @computer.device_type %>.</p>
    <% end %>
  </div>

  <%# ── Software section (read-only) ── %>
  <%# Lists software installed on this device. Each name links to the %>
  <%# software_items/show page for full detail. %>
  <%# Add/Edit/Delete from this page is deferred (Software feature Session F). %>
  <%# @software_items loaded by the controller: includes :software_name and %>
  <%# :software_condition, ordered by created_at ascending. %>
  <div class="mt-6">
    <h2 class="text-lg font-semibold mb-3">Software (<%= @software_items.count %>)</h2>

    <% if @software_items.any? %>
      <div class="bg-white border border-stone-200 rounded">
        <table class="min-w-full divide-y divide-stone-200">
          <thead class="bg-stone-50">
            <tr>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Name</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Version</th>
              <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Condition</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-stone-200 text-sm">
            <% @software_items.each do |item| %>
              <tr class="hover:bg-stone-50">
                <td class="px-4 py-3 whitespace-nowrap font-medium">
                  <%= link_to item.software_name.name, software_item_path(item),
                        class: "text-indigo-600 hover:text-indigo-900" %>
                </td>
                <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= item.version.presence || "—" %></td>
                <td class="px-4 py-3 whitespace-nowrap text-stone-600"><%= item.software_condition&.name || "—" %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-sm text-stone-500">No software installed on this <%= @computer.device_type %>.</p>
    <% end %>
  </div>

  <%# ── Back button ── %>
  <div class="mt-6">
    <a href="#"
       data-controller="back"
       data-back-fallback-url-value="<%= owner_path(@computer.owner) %>"
       data-action="click->back#go"
       class="text-sm text-stone-700 hover:text-stone-900">← Back</a>
  </div>

</div>
