<%# decor/app/views/software_items/new.html.erb - version 1.0 %>
<%# Session 46: Software feature Session D — new software item form. %>
<%# Pattern: identical structure to components/new.html.erb. %>
<%# Stimulus Back button: history.back() primary; owner's software sub-page as fallback. %>

<div class="max-w-5xl mx-auto">
  <h1 class="text-2xl font-semibold mb-4">Add Software</h1>

  <%= render "form", software_item: @software_item %>

  <%# Stimulus Back button — history.back() primary; software sub-page as fallback. %>
  <div class="mt-4">
    <a href="#"
       data-controller="back"
       data-back-fallback-url-value="<%= software_owner_path(Current.owner) %>"
       data-action="click->back#go"
       class="text-sm text-stone-700 hover:text-stone-900">← Back</a>
  </div>
</div>
