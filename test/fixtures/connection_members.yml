# decor/test/fixtures/connection_members.yml
# version 1.1
# v1.1 (Session 38): Added owner_member_id to all four fixtures.
#   Within each group, owner_member_ids are sequential starting at 1.
#   label is left absent (nil) — optional field, not set in test data.
#
#   Group bob_pdp8_vt100:
#     bob_pdp8_member  → owner_member_id: 1
#     bob_vt100_member → owner_member_id: 2
#
#   Group alice_pdp11_vax:
#     alice_pdp11_member → owner_member_id: 1
#     alice_vax_member   → owner_member_id: 2
#
#   Unique index (connection_group_id, owner_member_id) is satisfied:
#   IDs are unique within each group; two groups both having member IDs 1 and 2
#   is correct (the constraint is scoped to the group, not global).
# v1.0 (Session 31): Initial fixture data.

bob_pdp8_member:
  connection_group: bob_pdp8_vt100
  computer: bob_pdp8
  owner_member_id: 1

bob_vt100_member:
  connection_group: bob_pdp8_vt100
  computer: bob_vt100
  owner_member_id: 2

alice_pdp11_member:
  connection_group: alice_pdp11_vax
  computer: alice_pdp11
  owner_member_id: 1

alice_vax_member:
  connection_group: alice_pdp11_vax
  computer: alice_vax
  owner_member_id: 2
