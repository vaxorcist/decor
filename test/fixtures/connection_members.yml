# decor/test/fixtures/connection_members.yml
# version 1.0
# Session 31: Part 1 — Connections feature foundation.
# Four member rows forming two complete connection groups:
#
#   Group bob_pdp8_vt100 (bob's, 2 members):
#     bob_pdp8_member  — bob's PDP-8 computer
#     bob_vt100_member — bob's VT100 terminal (peripheral)
#
#   Group alice_pdp11_vax (alice's, 2 members):
#     alice_pdp11_member — alice's PDP-11/70
#     alice_vax_member   — alice's VAX-11/780
#
# UNIQUE constraint: (connection_group_id, computer_id) — each device appears
# at most once per group. All four rows satisfy this constraint.
# All devices in each group belong to the same owner (cross-owner integrity
# satisfied: bob's group has only bob's devices, alice's only alice's).

bob_pdp8_member:
  connection_group: bob_pdp8_vt100
  computer: bob_pdp8

bob_vt100_member:
  connection_group: bob_pdp8_vt100
  computer: bob_vt100

alice_pdp11_member:
  connection_group: alice_pdp11_vax
  computer: alice_pdp11

alice_vax_member:
  connection_group: alice_pdp11_vax
  computer: alice_vax
