# decor/test/fixtures/computers.yml
# version 1.8
# v1.8 (Session 25): Added charlie_dec_vt278 fixture for device_type: 2 (peripheral)
#   enum tests and owners#peripherals smoke test.
#   Assigned to owner three (charlie) — neutral owner with no hardcoded count
#   assertions in any test file.
# v1.7 (Session 21): Added barter_status values to two fixtures for filter tests.
#   dec_unibus_router: barter_status: 1 (offered) — charlie's appliance.
#   alice_vax:         barter_status: 2 (wanted)  — alice's computer.
#   All other fixtures: barter_status omitted → DB default 0 (no_barter).
#   barter_status values: 0 = no_barter, 1 = offered, 2 = wanted.
# v1.6: dec_unibus_router owner changed to three (charlie) — neutral owner.
# device_type values: 0 = computer (default), 1 = appliance, 2 = peripheral.

alice_pdp11:
  owner: one
  computer_model: pdp11_70
  serial_number: SN12345
  order_number: ORD-1985-001
  computer_condition: original
  run_status: working
  history: Originally used at MIT for student labs.

alice_vax:
  owner: one
  computer_model: vax11_780
  serial_number: VAX-780-001
  order_number: ORD-VAX-002
  computer_condition: original_repaired
  run_status: repair
  barter_status: 2
  history:

bob_pdp8:
  owner: two
  computer_model: pdp8
  serial_number: PDP8-7891
  order_number: ORD-2010-008
  computer_condition: modified
  run_status: working_problems
  history: Purchased from a collector in 2010. Some expansion cards replaced.

bob_vt100:
  owner: two
  computer_model: vt100
  serial_number: VT100-5432
  order_number: ORD-VT-004
  computer_condition: original
  run_status: working
  history:

# Computer with no association for testing deletion
unassigned_condition_test:
  owner: one
  computer_model: pdp11_70
  serial_number: TEST-001
  order_number: TEST-ORD-001
  computer_condition: built
  run_status: defective
  history:

# Appliance fixture for device_type enum tests.
# Represents an autonomous network device that operates without a host computer.
# Assigned to owner three (charlie) — neutral owner with no hardcoded count assertions.
# device_type: 1 = appliance.
# barter_status: 1 = offered — used for barter filter tests.
dec_unibus_router:
  owner: three
  computer_model: pdp11_70
  serial_number: RTR-001
  order_number: ORD-RTR-001
  computer_condition: original
  run_status: working
  device_type: 1
  barter_status: 1
  history: Standalone UNIBUS-attached routing device.

# Peripheral fixture for device_type enum tests and owners#peripherals smoke test.
# Represents a device that attaches to a host computer rather than operating
# autonomously. Assigned to owner three (charlie) — neutral owner.
# device_type: 2 = peripheral.
charlie_dec_vt278:
  owner: three
  computer_model: vt100
  serial_number: VT278-001
  order_number: ORD-VT278-001
  computer_condition: original
  run_status: working
  device_type: 2
  history: DEC VT278 DECmate personal computer / word processor.
