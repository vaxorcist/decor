# decor/test/fixtures/connection_groups.yml
# version 1.1
# v1.1 (Session 38): Added owner_group_id to both fixtures.
#   bob_pdp8_vt100  → owner_group_id: 1  (first group for bob)
#   alice_pdp11_vax → owner_group_id: 1  (first group for alice)
#   Each owner has only one group in the fixtures, so 1 is correct for both.
#   The unique index (owner_id, owner_group_id) is satisfied — different owners
#   may share the same owner_group_id value (it is only unique per owner).
# v1.0 (Session 31): Initial fixture data.

bob_pdp8_vt100:
  owner: two
  connection_type: rs232
  label: PDP-8 terminal connection
  owner_group_id: 1

alice_pdp11_vax:
  owner: one
  label: Lab setup
  owner_group_id: 1
